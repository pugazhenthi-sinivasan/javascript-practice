// 1.Write a program where a robot must complete 10 laps around a circuit. Use a while loop to count the laps and display the lap number each time the robot completes one.
let r=0;
while(r<=10)
{
    console.log(`robot complet${r} num`);
    r++;
}
// 2.Write a program where a robot must complete 10 laps around a circuit. Use a while loop to count the laps and display the lap number each time the robot completes one.
let better =20;
let e =1;
while(e<=20){
    if(better<=20){
        console.log("bettery is low");
        
    }}
    e++;